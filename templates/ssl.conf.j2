$SERVER["socket"] == "192.168.5.80:443" {
server.document-root = "/media/cloud/photos"
ssl.engine = "enable"
ssl.pemfile = "{{ SSL_cert_dir }}/{{ SSL_cert_name }}"
ssl.ca-file = "{{ SSL_cert_dir }}/{{ SSL_CA_name }}"
}
